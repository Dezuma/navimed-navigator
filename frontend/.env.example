VITE_NAVI_AI_ENDPOINT=http://127.0.0.1:8787/navi/respond
VITE_NAVI_CALLBACK_URL=http://127.0.0.1:8787/navi/callback
# Optional — only if tools/mock-ai sets NAVI_API_KEY (token is still exposed in the browser bundle; demo only).
# VITE_NAVI_API_KEY=
